[build]
  command = "npm run build"
  publish = "dist"
  base = "frontend"

[[redirects]]
  # This rule ensures all routes are handled by index.html (SPA)
  from = "/*"
  to = "/index.html"
  status = 200
